# 流量监控系统启动说明

本文档用于启动、关闭和检查本项目后台服务。

## 1. 固定访问地址

当前端口固定使用 `5102`，不要随意更换端口。

启动成功后，在浏览器打开：

```text
http://127.0.0.1:5102/
```

页面顶部显示“采集正常”，并且“更新时间”有最新时间，就说明后台正在正常工作。

## 2. 启动前准备

打开“终端”，进入项目目录：

```bash
cd /Users/jade/Downloads/network-traffic-monitor-main
```

确认项目里有 `.venv` 目录。如果有，直接按下面方式启动即可。

## 3. 前台启动

适合临时调试使用。终端窗口不能关闭，关闭后服务也会停止。

```bash
.venv/bin/python server.py
```

看到类似下面内容，表示启动成功：

```text
服务启动: http://127.0.0.1:5102
```

然后打开：

```text
http://127.0.0.1:5102/
```

## 4. 后台启动

适合日常使用。关闭终端窗口后，服务仍然继续运行。

```bash
cd /Users/jade/Downloads/network-traffic-monitor-main
nohup .venv/bin/python server.py > monitor.log 2>&1 &
```

启动后查看日志：

```bash
tail -f monitor.log
```

看到“采集成功”或“服务启动: http://127.0.0.1:5102”，说明服务已经运行。

## 5. 关闭服务

如果是前台启动，在终端里按：

```text
Control + C
```

如果是后台启动，先查看 `5102` 端口是谁占用：

```bash
lsof -nP -iTCP:5102 -sTCP:LISTEN
```

会看到类似：

```text
Python  12345  jade  ...  TCP 127.0.0.1:5102 (LISTEN)
```

其中 `12345` 是进程号。关闭它：

```bash
kill 12345
```

如果还没关闭，再执行：

```bash
kill -9 12345
```

关闭后再次检查：

```bash
lsof -nP -iTCP:5102 -sTCP:LISTEN
```

如果没有输出，表示 `5102` 已经空出来。

## 6. 端口被占用怎么办

如果启动时看到：

```text
Address already in use
Port 5102 is in use by another program
```

说明旧后台还在运行。按第 5 节先关闭旧进程，然后重新启动。

注意：这个项目要求端口保持 `5102`，不要为了绕过占用问题改成别的端口。

如果执行 `kill` 提示 `operation not permitted`，通常是旧服务在另一个终端、工具或后台会话里启动的。可以用下面任一方式处理：

- 找到之前启动服务的终端窗口，按 `Control + C`
- 打开“活动监视器”，搜索 `Python`，结束对应进程
- 重新打开终端后再执行 `lsof` 和 `kill`

## 7. 验证接口是否正常

浏览器验证：

```text
http://127.0.0.1:5102/
```

命令验证：

```bash
curl http://127.0.0.1:5102/api/stats
```

如果返回一段 JSON 数据，说明后台接口正常。

检查 H3C ARP 绑定数据：

```bash
curl "http://127.0.0.1:5102/api/h3c/arp-bindings?field=ip&q=192.168.20.247&refresh=1"
```

如果能返回 IP、MAC、员工姓名，说明 H3C ARP 绑定表读取正常。

## 8. 常见问题

### 页面打不开

先确认后台是否启动：

```bash
lsof -nP -iTCP:5102 -sTCP:LISTEN
```

如果没有输出，说明后台没运行，需要重新启动。

### 页面一直不是最新

浏览器刷新页面。必要时关闭旧后台，重新启动新版本。

### 没有员工姓名

员工姓名来源于 H3C 路由器：

```text
http://192.168.20.1/home.asp
```

位置：安全专区 -> ARP安全 -> ARP绑定表。

其中“描述”字段就是员工姓名，系统会按员工内网 IP 和 MAC 进行匹配。

### “绑定员工信息”没有显示到页面

先确认后台是最新版本，并且端口 `5102` 没有被旧进程占用。

再确认本地绑定接口：

```bash
curl http://127.0.0.1:5102/api/mac-bindings
```

如果接口里能看到对应 IP、MAC、员工姓名，刷新页面后员工内网表应显示该姓名。

## 9. 推荐日常操作顺序

1. 先关闭旧服务。
2. 再后台启动服务。
3. 打开 `http://127.0.0.1:5102/`。
4. 页面显示“采集正常”后开始使用。

常用命令汇总：

```bash
cd /Users/jade/Downloads/network-traffic-monitor-main
lsof -nP -iTCP:5102 -sTCP:LISTEN
nohup .venv/bin/python server.py > monitor.log 2>&1 &
tail -f monitor.log
```
